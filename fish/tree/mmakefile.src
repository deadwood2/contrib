
include $(SRCDIR)/config/aros-contrib.cfg

NOWARN_FLAGS := \
    $(NOWARN_OLD_STYLE_DEFINITION)

USER_CFLAGS := $(NOWARN_FLAGS)

%build_prog mmake=fish-tree \
    progname=tree targetdir=$(AROS_CONTRIB)/Misc/fish \
    files=tree
